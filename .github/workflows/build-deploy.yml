name: Build and Deploy Code

on: [push, pull_request]

jobs:
  job1:  
    runs-on: ubuntu-latest #which Operating system to run on
    steps:
      - name: pulling git repository
        uses: actions/checkout@v3
      - name: say hi to eshaan
        run: echo "hello eshaan"